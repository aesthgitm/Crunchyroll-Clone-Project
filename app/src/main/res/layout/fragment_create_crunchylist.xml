<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#111315"
    android:clickable="true"
    android:focusable="true">

    <!-- Top Toolbar -->
    <RelativeLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:paddingHorizontal="16dp">

        <ImageView
            android:id="@+id/btn_close"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:padding="8dp"
            android:src="@drawable/ic_close"
            app:tint="@color/white"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Close" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/btn_close"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:text="Buat Crunchylist"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:fontFamily="@font/poppinsbold" />

    </RelativeLayout>

    <!-- Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:layout_marginTop="180dp">

        <!-- Name Input -->
        <EditText
            android:id="@+id/edt_list_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Nama Daftar"
            android:textColorHint="#8E9297"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:background="@null"
            android:maxLines="1"
            android:singleLine="true"
            android:inputType="text"
            android:fontFamily="@font/poppinsregular"
            android:paddingVertical="8dp" />

        <!-- Underline -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#8E9297"
            android:layout_marginTop="2dp" />

        <!-- Create Button -->
        <Button
            android:id="@+id/btn_create"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="40dp"
            android:background="@drawable/bg_button_create_outline"
            app:backgroundTint="@null"
            android:text="Buat daftar"
            android:textColor="@color/color_btn_create_text"
            android:textSize="14sp"
            android:fontFamily="@font/poppinsbold"
            android:textAllCaps="false"
            android:enabled="false" />

    </LinearLayout>

</RelativeLayout>
