<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_black"
    android:padding="24dp">

    <!-- Container for center content to align exactly as in screenshots -->
    <LinearLayout
        android:id="@+id/center_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toTopOf="@id/action_button"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed">

        <!-- Illustration Image -->
        <ImageView
            android:id="@+id/illustration"
            android:layout_width="260dp"
            android:layout_height="260dp"
            android:scaleType="fitCenter"
            android:src="@drawable/cat_tv"
            android:contentDescription="Illustration" />

        <!-- Title / Primary Text -->
        <TextView
            android:id="@+id/title_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="Daftar tontonmu butuh perhatian."
            android:textColor="@color/white"
            android:textSize="17sp"
            android:textStyle="bold"
            android:gravity="center" />

        <!-- Subtitle / Secondary Text -->
        <TextView
            android:id="@+id/subtitle_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Ayo isi dengan anime keren."
            android:textColor="@color/tab_unselected_grey"
            android:textSize="13sp"
            android:gravity="center" />

    </LinearLayout>

    <!-- Regular Orange Button -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/action_button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="32dp"
        android:background="@drawable/bg_button_orange"
        android:text="Jelajahi Semua"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Premium Yellow/Gold Button -->
    <LinearLayout
        android:id="@+id/premium_button_container"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="32dp"
        android:background="@drawable/bg_button_gold"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/ic_crown"
            app:tint="@color/black" />

        <TextView
            android:id="@+id/txt_premium_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Jadilah Premium"
            android:textColor="@color/black"
            android:textSize="15sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Populated Content (RecyclerView or List Container) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_populated_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:scrollbars="vertical" />

</androidx.constraintlayout.widget.ConstraintLayout>
